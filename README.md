# Lilies in the box

### v0.3.50 对话组件工具与拖动诊断收口

- ChatGPT/Codex 对话现在只动态公开 27 个经审核的本地组件工具，覆盖任务、专注、阅读、提醒、成长、衣柜与盒中世界，并保持在 32-function 上限内；Calendar、Slack、凭据、Shell 与任意注册表动作不进入该工具面。
- 工具参数继续由组件注册表执行递归 schema 校验，并复用既有权限、审计与执行链；结果只写入有界审计字段，成功调用会刷新对应的 QML 投影，未知命名空间与未审核动作一律拒绝。
- 无内容运行快照增加有界的 `dragProxy` 状态、计数和失败原因；原生代理拖动不再无条件抬高 Qt Quick 动画预算，同物理尺寸与 DPR 的静态缓存可桥接一次手势，未就绪的 0×0 源以 `source-not-ready` 明确拒绝。
- 版本资源、主题、安装器、构建与四份候选证据统一为 v0.3.50；`promote_v0350.ps1` 以 SHA-256 固定 v0.3.49 及更早历史包装器，并可通过 `promote_v0350.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.49 高 DPI 拖动快照与按下热路径收口

- 修正高 DPI 下拖动代理位图的 DPR² 尺寸误差；异步快照增加 gesture fence，旧手势完成结果不能覆盖当前缓存。
- 按下热路径移除磁盘写入与快照重活，红绳 reflow 合并到显示帧；释放后只保留一帧 preview 桥接真实 Qt Quick 画面。
- 版本资源、主题、安装器、构建与四份候选证据统一为 v0.3.49；`promote_v0349.ps1` 以 SHA-256 固定 v0.3.48 及更早历史包装器，并可通过 `promote_v0349.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.48 分层拖动代理与局部手势热路径

- 人物拖动会复用空闲期缓存的 QML 透明快照，通过 Win32 `UpdateLayeredWindow` 显示轻量代理；拖动期间由 DWM 移动代理，真实 Qt Quick 根窗口只在释放时提交一次最终位置，避免整棵场景图跟随高轮询鼠标重绘。
- 缩放柄、红绳盒子和功能按钮的 `resize/accessory/action` 局部手势改由 `FrameAnimation` 按显示帧合并最新位移；激活期间暂停无用的 Qt 与原生命中过滤器，释放时一次性批量上报计数，不在原始事件热路径反复跨入 Python。
- `pet-drag-latest.json` 的诊断升级为 `schema 3`，记录局部手势时长、原始事件、场景提交和原生几何提交次数，不保存坐标、标题或文字；人物的既有系统拖动诊断字段继续兼容。
- 代理预览隐藏改为可取消的成员 `QTimer`，并绑定 session `generation` 与单调时钟截止点；快速开始第二次拖动时，第一次拖动的旧回调不能隐藏新代理。代理原生移动启动失败时会恢复根窗口物理原点，再安全回退到普通 Windows 系统移动。
- 版本资源、主题、安装器、构建与四份候选证据统一为 v0.3.48；`promote_v0348.ps1` 以 SHA-256 固定 v0.3.47 及更早历史包装器，并可通过 `promote_v0348.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.47 原生拖动事件链与人物边缘命中

- Windows 原生拖动改由进程内 `EVENT_SYSTEM_MOVESIZESTART/END` 事件确认开始与结束；窗口句柄按指针宽度传递，避免 64 位 HWND 溢出后静默丢失完成事件。
- 原生移动期间移除逐帧 Python 输入回调，暂停 75ms 场景投影与输入脉冲；首次位移锁存后只保留低频异常恢复，运行心跳与 Explorer 恢复监视器继续工作。
- 人物轮廓增加 6 个逻辑像素的圆形容错命中，QML 与 Win32 共用同一遮罩；发梢、裙边更容易抓住，同时大面积透明区域和四角继续穿透到底层应用。
- `pet-drag-latest.json` 从按下阶段开始记录无内容的 `armed/native-active/finished` 状态；即使完成回调丢失，也能区分未命中、未进入原生移动和释放链异常。
- 隐藏 qwindows 发布探针会真实触发并验证原生 START/END 事件、64 位 HWND、透明穿透和无焦点窗口；四份候选证据统一为 v0.3.47，并由 `promote_v0347.ps1 -ValidateOnly` 重放发布门禁。

> v0.3.46 性能说明：GUI 线程现在复用一条显式、线程隔离的 SQLite 会话；每秒生产力刷新不再为每个投影反复打开 F 盘数据库，后台工作线程仍使用各自独立连接。

### v0.3.46 拖动交互屏障与后台协作收口

- 原生拖动建立统一交互屏障：移动期间暂停低优先级陪伴计时、屏幕截图、模型探测、在线内容刷新与记忆归档；已排队的陈旧生成结果不能在拖动中突然创建气泡。
- 窗口目录的 Win32 枚举支持协作取消；开始拖动时终止正在进行的后台快照，显式刷新合并为一次释放后重跑，避免 Python 回调争用 GUI 线程又不丢用户刷新请求。
- 非桌宠 QML 窗口的资源生命周期改用 `visibleChanged` 信号，不再让通用事件过滤器接收每次鼠标、缩放和绘制事件；透明桌宠同时关闭多余的 DWM 矩形阴影。
- `pet-drag-latest.json` 现在也保存未移动手势的无内容诊断，使原生移动被拒绝、回退路径卡顿和“尚未实际拖动”可以区分；诊断仍只在释放后由单独写入线程落盘。
- 拖动后的陪伴、窗口目录和模型能力恢复都有安静期与状态重验；用户对话、划词等高优先级能力不被陈旧的主动观察任务替代。
- 版本资源、主题和四份候选证据统一为 v0.3.46；`promote_v0346.ps1` 以 SHA-256 固定 v0.3.45 及更早历史包装器，并可用 `promote_v0346.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.45 原生拖动热路径净空

- 原生拖动开始后同时暂时卸下应用级命中过滤器和桌宠窗口自己的 Qt 事件过滤器；500／1000Hz 鼠标移动不再跨入 Python 或 QML，松手由有界 watchdog 恢复两条过滤链并完成同一手势。
- 按下会冻结正在运行的窗口位移动画、人物分层姿态与高分辨率交叉淡入；拖动期间仍保持精确抓点，释放后才执行栖息脱离和姿态收尾。
- 划词的 30ms 鼠标轮询、每秒任务数据库维护、Explorer 恢复检查和窗口目录提交都会避开拖动锁；窗口目录枚举移到后台，旧快照不能覆盖新的活动窗口状态。
- 隐藏 QML 窗口的显存资源回收增加 420ms 安静期，拖动期间不会释放或重建光环等场景图；常用人物与盒子命中走快速路径，不再先遍历整棵控件树。
- `pet-drag-latest.json` 继续只记录无内容的模式、耗时、刷新率和完成来源；异常按键状态、窗口句柄重建、滤镜恢复重试以及移出再回原位都纳入回归门禁。
- 版本资源、主题和四份候选报告统一为 v0.3.45；`promote_v0345.ps1` 以 SHA-256 固定 v0.3.44 及更早历史包装器，并可用 `promote_v0345.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.44 原生拖动单一移动权与释放后姿态收尾

- Windows 原生拖动在按下阶段就锁定系统移动事务；原生启动 pending 与 active 期间直接回退均停止提交窗口坐标，让一次手势始终只有一个移动权，不再出现 DWM 与 QML 同帧争抢位置。
- 原生拥有手势后会在读取全局坐标或进入 QML 前直接消费高频 `MouseMove`，500／1000Hz 鼠标不再把无用采样和属性写入压到 GUI 线程；松手、取消和序列确认仍保持完整。
- 人物在整个拖动中以 `interactionSnap` 保持按下时的栖息姿态与抓点，不在首次位移时脱离或重映射；原生路径必须等到 Windows `WM_EXITSIZEMOVE` 后才执行一次脱离与姿态过渡，避免嵌套移动循环中重建场景图或人物相对光标跳动。
- 去掉随窗口每次 `x/y` 变化执行的 QML 热回调，改由原生 RECT 路径累计位移并保留逻辑 4px 防抖；按下时同步停止菜单轨道与盒面旋转，移动期间不再携带未结束动画。
- 新增 content-free 的 `runtime/pet-drag-latest.json` 诊断，只在真实移动后延迟记录移动模式、时长、刷新率与抑制／提交计数，不保存坐标或窗口内容；拖动 verifier 与契约测试覆盖 pending 回退、释放早于原生退出、双轴往返、延迟脱离和单次收尾。
- 版本资源、主题和四份候选报告统一为 v0.3.44；`promote_v0344.ps1` 以 SHA-256 固定 v0.3.43 及更早历史包装器，并可用 `promote_v0344.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.43 165Hz 原生拖拽与高刷新率回退

- 未保存过拖拽偏好的安装现在默认使用 Windows 原生系统拖动，让 DWM 直接跟随 165Hz 指针与显示刷新；用户明确保存的 `direct` 模式仍会保留，系统拖动启动失败时也会安全回退。
- `direct` 回退不再由固定 16ms 定时器限制在约 60Hz，而是使用 `FrameAnimation` 随 Qt Quick 实际渲染帧消费最新坐标，在高刷新率屏幕上避免隔帧跳动。
- 原生事件过滤器在手动拖动事务中只保留最新全局坐标，并抑制中间原始 `MouseMove` 继续涌入 QML；松手事件仍完整放行，最终位置不会丢失。
- 拖动期间窗口相关布局统一读取冻结的展示坐标与工作区，功能按钮、气泡、盒中世界和状态层不再因为窗口逐帧移动而反复重算绑定。
- 版本资源、主题和四份候选报告统一为 v0.3.43；`promote_v0343.ps1` 以 SHA-256 固定 v0.3.42 及更早历史包装器，并可用 `promote_v0343.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.42 桌宠拖拽流畅度与公开仓库卫生

- `direct` 仍是默认拖拽方式，但高频指针事件不再逐次推动原生窗口：事件只覆盖保存最新坐标，由显示帧按帧合并提交；松手前强制消费末坐标，1000Hz 输入也不会形成排队追赶。需要时仍可在设置中选择 Windows 系统拖动。
- 按下时缓存当前显示器工作区，同屏拖动不再反复跨 QML／Python 查询；跨屏时才刷新边界。拖动期间暂停几何心跳与依赖工作区的面板重算，松手后延迟持久化最终位置，减少途中和收尾卡顿。
- 原生命中测试在拖动事务内走固定客户区路径，后台模型与桌面感知任务也避开这段高交互窗口；人物仍保持透明区域穿透、缩放与跨 DPI 约束。
- 公开仓库卫生已收口：运行数据、凭据、构建产物、发布备份和本机诊断不进入 Git，安装与恢复脚本不再固化个人用户名路径；`LiliesInTheBox.spec` 继续作为可复现构建输入保留。
- 版本资源、主题和四份候选报告统一为 v0.3.42；`promote_v0342.ps1` 以 SHA-256 固定 v0.3.41 及更早历史包装器，并可用 `promote_v0342.ps1 -ValidateOnly` 重放相同发布门禁。

### v0.3.41 屏幕感知回执、自然哲思与频率输入收口

- 屏幕感知的自定义频率继续使用独立编辑草稿：逐位输入时不会被心跳或无关设置刷新跳回旧值；应用时按 `5–180` 分钟与每日 `1–50` 条夹取，冲突、失败恢复和跨重启均有真实 QML 交互验证。
- “取到画面、把像素交给模型、模型采用画面、气泡最终呈现”拆成互不覆盖的回执。窗口切换、隐私静默、重复抑制或原生窗口未曝光时，仍会诚实保留像素是否已使用，并单独说明最终呈现结果。
- 图像气泡用 content-free 的会话与气泡 ID 关联重启恢复；旧未读气泡不能冒领新截图。诊断写入失败不再阻断气泡 ACK、TTL、隐私清理或截图内存释放。
- 无截图生成不再借用颜色、位置、前景、界面等词伪装看见屏幕；有画面的哲思必须在详情中说明真实关系、条件或机制。封闭鸡汤、复述式模板和超过两句的拥挤摘要会被安静拒绝，新闻与科研仍服从来源元数据边界。
- 设置页显示受控的 Luna／Terra 名称、模型报告的画面把握和最终呈现状态；版本资源与四份发布报告统一为 v0.3.41。新包装器以 SHA-256 固定 v0.3.40、v0.3.39 与 v0.3.38 历史；可用 `promote_v0341.ps1 -ValidateOnly` 重放同一发布门禁。

### v0.3.40 空闲预算、QML 发布门禁与打包体积收敛

- 高频原生窗口泵不再读取生产力 SQLite 状态；空闲生产力调度按需发布变更，并在提醒、叙事或 outbox 已提交后发生短暂失败时保留下一次刷新义务。活动专注与阅读仍维持一秒级可见时钟，专注偏离按低频调度且每次状态只投递一次。
- 陪伴频率草稿、即时组件变更、Calendar 对账与模型返回 envelope 的刷新边界补齐回归契约，避免后台心跳造成整页无效重绘，同时保证真实变更及时出现在界面。
- PyInstaller 使用项目内的选择性 `PySide6.QtQml` hook，只收集应用实际导入的 QML 模块并对缺失模块 fail closed；WebEngine、Quick3D、Charts、Graphs、DataVisualization、Location、Pdf 与 WebSockets 等未使用家族不再进入候选包。
- 打包自检在加载主 QML 前收集警告，只有 `qmlWarningCount = 0` 且警告数组为空才通过。紧凑资源报告记录候选目录总字节数、文件数与禁用 Qt 家族扫描结果，promotion 会现场重新计算，伪造或陈旧证据不能绕过门禁。
- 版本资源、安装元数据、主题清单与四份发布报告统一升至 v0.3.40；新包装器以 SHA-256 固定 v0.3.39 历史，并继续核对 v0.3.38 的已知 SHA。正式提升与 `-ValidateOnly` 共用完整候选证据和 formal checksum fence。

### v0.3.39 单次屏幕观察、自然表达与频率稳定性

- 屏幕感知的自定义频率不再被心跳或偏好刷新跳回预设值；分钟数和每日上限以独立草稿编辑，应用时只按允许的下界与上界钳制，保存失败也不会用旧值静默覆盖当前输入。
- 新增“观察当前窗口一次”：设置窗先收起，350ms 后只截取一次经隐私检查的非浏览器活动窗口；授权、前景身份、截图或图像模型任一环节失败都会诚实停止，绝不用泛化台词冒充已经看见屏幕。
- 模型回复失败时保留原气泡并清除忙态，不再使用固定角色句。关闭、暂停与切换类别会使迟到结果失效，“换一个”保持当前类别，回复后的来源明确标为起始来源。
- “哲思”在有具体画面锚点时允许自然陈述，不再强迫问号和“不是……而是……”等套路；仍拒绝鸡汤式建议，详情必须增加关系、机制或尺度，不能复述标题。
- 最近展示内容按“摘要 + 详情”组合跨重启去重；新闻与科研内容缺少有效来源、日期或 HTTP(S) 链接时不会交给模型伪装成实时内容。
- Slack Socket Mode 改用隔离在后台线程中的 Python 标准库 WSS 传输，不再加载不稳定的 QtWebSockets；TLS 主机校验、Upgrade、掩码、分片、ping/pong、长度上限和“先 ACK 再交主线程”均有协议测试，不新增依赖。
- SQLite 迁移会精确清理临时备份及其 `-wal`/`-shm` 边车，并排除可重建缓存而保留普通用户文件。版本元数据与四份发布报告统一为 v0.3.39；发布包装器以 SHA-256 固定 v0.3.38 历史。

### v0.3.38 频率草稿冲突门禁与线程归属

- 自定义频率草稿只在已提交的频率投影确实从基线变化时进入冲突态；普通“应用”会阻止覆盖并保留当前输入，外部更新不会静默覆盖当前草稿，可选择“恢复最新”或明确点击“仍然应用”。
- 直接应用仍会读取尚未失焦的可见文本并按既有边界提交；兴趣、类别权重等非频率偏好更新不会误报频率冲突，也不会打断正在编辑的草稿。
- 频率写入由 `CompanionController` 的 Qt 所有者线程串行化；后台线程直接调用会在读写偏好、数据库或发布信号前被拒绝，跨线程调用必须先排队回所有者线程。
- 版本元数据与四份发布报告统一升至 v0.3.38；新包装器以 SHA-256 固定 v0.3.37 历史，并继续复用完整静态发布门禁。

### v0.3.37 陪伴频率失败恢复与旧库投影自愈

- 自定义频率直接“应用”会读取仍聚焦的可见文本并按既有边界提交；持久化失败不会用旧值覆盖当前草稿，成功提交只发布一次一致的偏好快照。
- 启动时以当前生效的自定义频率为权威，在同一连接和事务内回填缺失或陈旧的 remembered 投影；预设频率仍保留上次自定义值。
- 版本元数据与四份发布报告统一升至 v0.3.37；新包装器以 SHA-256 固定 v0.3.36 历史，并继续复用完整静态发布门禁。

### v0.3.36 陪伴频率原子设置与信号边界

- 陪伴偏好改用独立 `preferencesChanged` 通知；运行状态心跳不再误触发设置页整块重同步，频率草稿可在编辑期间保持稳定。
- 自定义频率会先按 `5–180` 分钟、每日 `1–50` 条归一化，再以一次数据库事务同时保存偏好与自定义值；失败不发布半成品内存状态或信号，成功只通知一次。
- 直接点击“应用”会读取仍聚焦的可见文本；新增信号拆分、原子失败回滚、边界夹取与重启 round-trip 契约测试。
- 浏览器像素观察在 v0.3.36 继续暂停；版本资源与四份报告统一升至 v0.3.36，新包装器以 SHA-256 固定 v0.3.35 历史。

### v0.3.35 运行恢复可观测性与发布围栏

- 屏幕陪伴会把用户兴趣与兴趣／场景比例交给模型，并对哲思质量、视觉锚点和内容关联做结构化校验；主观内容在模型不可用时保持安静，不再用预设句伪装生成结果。
- 自定义陪伴频率在逐位输入和后台心跳期间保持原始草稿；直接点击“应用”会读取尚未失焦的可见文本，再按 `5–180` 分钟与每日 `1–50` 条的边界保存，避免旧值把新输入覆盖回去。
- 紧凑桌宠的初遇重播、径向菜单命中、专注／阅读／衣橱定位和状态提示统一修正；功能按钮从出现起就拥有一致的原生命中区域，Dock 激活窗口失败时保留当前抽屉并明确提示。
- 任务、提醒与连接器轮询增加异常边界和自动重试；单次刷新或 Calendar 后台线程启动失败不会让后续周期任务永久停止。
- 运行快照补齐 Qt 主线程心跳健康度，后台诊断读取保持线程安全且不暴露聊天、标题、路径或异常正文；应用退出时同步停止心跳计时器。
- 陪伴投递与恢复路径增加独立契约覆盖，单实例、后台维护和截图暂存失败保持安静、可恢复，并由内容无关测试验证。
- 浏览器像素观察在 v0.3.35 继续暂停；浏览器活动只提供粗粒度应用类别信号，不读取或保存页面像素。
- 版本资源、安装元数据、主题清单及四份打包诊断报告统一升至 v0.3.35；新发布包装器以 SHA-256 固定 v0.3.34 历史，并让正式提升与 `-ValidateOnly` 共用完整证据门禁。

### v0.3.34 原生窗口捕获探针与发布脚手架

- 屏幕陪伴不再把截图失败、近乎空白的画面或低置信图像结果退成一条貌似看过屏幕的通用文案；自动观察会安静地短时重试，显式请求只显示真实状态。
- 同一场景会轮换观察镜头；科普、吐槽、笑话、哲思与盒中世界使用不同的表达约束。只有模型给出具体视觉锚点且结构完整时，气泡才会被认定为屏幕观察。
- 对普通取图返回空白的窗口增加隔离的 `PrintWindow(PW_CLIENTONLY)` 后备取图：只请求当前已复核 HWND 的客户区，并在短生命周期隐藏进程内完成限时取图、缩放与编码；进程、窗口可见性或前景身份变化时立即丢弃，绝不改截整张桌面。
- v0.3.34 暂停浏览器像素观察；普通页、登录页和隐私页都只提供“网页浏览”应用类别信号。设置中的手动场景陪伴同样不截图，避免把粗粒度推测包装成看见了页面。
- 新增内容无关的原生窗口捕获探针，仅验证 Lilies 自有离屏窗口的调用、尺寸和视觉证据，不保存像素或读取用户窗口内容。
- 版本资源、安装元数据、主题清单与打包诊断报告指针统一升至 v0.3.34；安装前会先核对 EXE 的 `FileVersion`/`ProductVersion`，发布包装器以 SHA-256 固定 v0.3.33 历史，并让正式提升与 `-ValidateOnly` 共用完整证据门禁。

### v0.3.33 陪伴恢复、Dock 与设置交互更新

- 主动陪伴补齐恢复语义：旧截图失败状态安全迁移，损坏或过期的未读投递不会永久阻塞新内容；自动模型降级保持安静并短时重试，显式“换一个”仍可得到安全回应。
- Dock 的应用固定与取消固定改为真实可用操作，搜索与全部窗口路径统一刷新；连接失败明确展示可重试状态，不再把失败误报成已经连接或无响应。
- Slack 连接入口使用稳定锚点定位，滚动和窗口尺寸变化后仍能落到正确设置区；径向菜单与屏幕边缘、Dock、气泡之间增加防撞约束，避免常用入口被遮挡。
- 设置页重新梳理字号、行距、分组留白与说明文本，可读性和窄窗口回流同步改善；陪伴、连接器和桌面选项保留清晰的当前状态与下一步操作。
- 版本资源、安装元数据、主题清单与三份打包报告统一升至 v0.3.33；v0.3.32 发布包装器由 SHA-256 历史门禁保护，不参与后续修改。

### v0.3.32 姿态解析、未读入口与边界交互更新

- 新增独立的 `V03PetPoseResolver`，统一栖息、对话、选择、阅读、专注与衣柜姿态的优先级，并把高动态姿态纳入紧凑窗口的帧率判定。
- 超宽矮窗现在使用带宽度、纵横比和 DPI 回滞的全景栖息姿态；人物拖动在静默或受保护场景隐藏前提交最终位置，专注计时补齐 59/60/61 秒分钟边界验证。
- 未读陪伴消息在人物层保留可点击提示，并由原生命中遮罩、110 DIP 最小尺寸入口和现有控制器回归共同覆盖。
- 主动陪伴新增“哲思”类别；授权截图生成必须先给出具体、非敏感的视觉锚点，低信息画面或低置信结果会诚实退回应用类别，不会凭空补写视觉事实。
- 最近气泡摘要会跨重启参与近似去重，本地备用文案耗尽时保持安静；重复后的唯一重试通过 `variation_nonce` 切换观察角度，近期摘要签名按请求内加盐，不能跨请求关联原文。
- 类别权重采用平滑轮转；自定义频率的间隔与日上限支持直接键入，编辑锁只在可见页面的未提交草稿期间生效，不会被 heartbeat 或外部频率变更覆盖；隐藏页面会释放锁并重新同步最新已提交值。
- 浏览器默认只提供应用类别；像素截图需要独立确认的单次授权，撤销或关闭会围住原始图、暂存图和迟到结果，诊断只记录尝试、提交与接受阶段，不保存私密错误细节。
- 版本资源、安装元数据、主题清单与三份打包报告统一升至 v0.3.32；v0.3.31 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.31 F 盘 Qt 缓存边界与发布探针更新

- QML 字节码缓存与 Qt Quick RHI 管线缓存现在会在 `QApplication` 和 QML 引擎创建前显式路由到 `F:\code\Lilies in the box\private-data\cache`；继承的外部缓存路径会被覆盖，不再静默写入 `%LOCALAPPDATA%`。
- 独立的旧式 OpenGL shader 磁盘缓存保持关闭；F 盘数据根不可用时会清除强制缓存与显式路径变量并失败关闭，只进入不写缓存的恢复流程。
- 隐藏的 Windows 冷启动探针增加空白 `LOCALAPPDATA` 哨兵，并同时要求自检与原生启动报告中的 `qtCacheRouting` 路径、环境应用和禁用状态全部通过，缓存回退会直接阻断发布。
- 透明人物素材、姿态点击遮罩、专注动画四阶段和桌面原生命中门禁继续保留；带烘焙棋盘的失败草稿只允许位于 `art-reference`，不得进入运行时主题资产。
- Windows 版本资源、安装元数据、主题清单与三份打包报告统一升至 v0.3.31；v0.3.30 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.30 专注提醒归属与姿态切换完整性更新

- 专注偏离提醒现在严格绑定产生它的专注会话与有效期限；旧会话、已结束会话和到期提醒不能跨会话继续显示或处理动作，临界时刻的检查与投递保持同一归属。
- 人物姿态成图切换采用完整提交语义：新图尚未就绪时保留可见的旧姿态与对应命中遮罩，图像、镜像状态和点击区域在同一次切换中对齐，避免短暂空白或透明区域误拦截。
- 发布门禁继续继承桌面原生命中、专注动画四阶段、透明素材与姿态点击遮罩检查，并新增专注提醒竞态及姿态切换连续性的回归验证。
- Windows 版本资源、安装元数据、主题清单与三份打包报告统一升至 v0.3.30；v0.3.29 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.29 专注动效、桌面回返与响应式姿态更新

- 专注开始、暂停、恢复和结束现在由真实 QML 计时环逐阶段呈现；打包自检会验证启动脉冲、暂停冻结、恢复不重播和结束反馈，并要求四次状态序列严格递增。
- 完整 Lilies 桌面增加常驻的一键往返入口和桌面表面自愈探测；紧凑桌宠与完整桌面可直接互相切换，不再依赖临时窗口状态。
- 栖息姿态扩展为九种按窗口尺寸和可用边缘选择的程序化构图，覆盖极小、窄标题栏、中窗、宽窗、全景与屏幕边缘；六套服装均保留分层呼吸和接触锚点。
- 独立姿态素材门禁升级为规范化点击遮罩；透明像素默认穿透，镜像姿态同步镜像命中区域，缺失或伪透明素材继续保持 dormant，不进入运行时。
- Windows 版本资源、安装元数据、主题清单与三份发布报告统一升至 v0.3.29；v0.3.28 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.28 原生点击、跟手拖动与主动观察更新

- 真实“开始专注”按钮现在会立即呈现 880ms 启动脉冲；即使人物已经回到低功耗，脉冲、角度与活动指示仍清晰可见，结束后稳定回到 15 FPS，不会长期占用 60 FPS。
- 莉莉丝桌面新增有界的原生呈现探针，会核对 Qt 曝光、最小化、DWM 隐藏与同 Explorer 进程的 Progman/WorkerW 层级；失败时只做一次 `showNormal` 和一次 `hide/show`，不抢焦点。
- 全屏或敏感场景中的“展开桌面／显示当前界面”请求现在合并为单一 pending；离开静默状态后只重放一次，并确保 `always` 模式的莉莉丝回到桌面。
- 主动陪伴增加不含窗口标题的应用级策略列表，可明确设置静默、观察或允许气泡；敏感应用仍强制 fail-closed。截图编码可取消、模型任务前台更换和未读投递也继续保持可验证收敛。
- 盒中世界、对话、设置和从功能库选出的陪伴入口现在都经过实际 QML 点击链；Windows 隐藏启动门禁进一步核对人物区、径向按钮、透明角与 `WS_EX_NOACTIVATE` 原生样式，入口不再只验证后端状态变化。
- 人物拖动改为一次提交双轴窗口位置，按住时冻结会干扰抓点的局部姿态；盒子使用真实窗口边界，缩放会反向校正窗口原点，让被抓住的手柄持续贴着全局光标。主动截图在排队、调用前和呈现前都会重新核对同一窗口与标题，旧文档结果不会误弹到新文档上。
- 对话、主动陪伴和记忆归档统一增加提交屏障：排队时冻结所选模型，取消或关闭会立即拒绝并唤醒确认等待；迟到回复不会再保存或显示，记忆分类也只在任务仍有效时写入数据库。
- Windows 版本资源、安装元数据、主题清单与三份发布报告统一升至 v0.3.28；v0.3.27 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.27 拖动连续性与主动陪伴恢复更新

- 桌宠拖动会记住人物内部的真实抓点；从栖息姿态脱离并重排为站立姿态时，同一身体部位仍连续跟随光标，不再先跳一下再追上。手动脱离按应用身份保持：同一应用的多个窗口或 Lilies 自身界面短暂置前不会把她强行吸回去，确认切换到其他外部应用后才恢复自动栖息。
- 主动陪伴不再把所有全屏窗口一概当成游戏：非游戏全屏仍可按用户策略观察和呈现，游戏、敏感与受保护场景继续静默。未成功呈现或进程重启留下的未读气泡可以安全重投，只有原生窗口真实可见并收到 ACK 后才开始阅读时限。
- 用户撤销智能观察时会取消正在排队或生成中的观察任务，并清理截图暂存；运行状态会明确区分截图授权、当前截图阶段与模型图像能力，能力状态可在桥接变化后重新检测，不再长期卡在旧结果。
- Windows 版本资源、安装元数据、主题清单与三份发布报告统一升至 v0.3.27；v0.3.26 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.26 全服装分层栖息姿态更新

- 极小窗口使用“抓角”栖息，中型窗口使用“垂挂”栖息；两种姿态均由现有身体、头发、脸、手臂、裙装、裂光与阴影图层程序化重组，因此六套服装都能保持正确的分层、锚点与呼吸动画。
- 可选姿态位图在透明度、裁切和清晰度门禁通过前继续保持 dormant；运行时始终有可验证的分层回退，不会因缺少新图而退回同一张旧姿态。
- Windows 版本资源、安装元数据、主题清单与三份发布报告统一升至 v0.3.26；v0.3.25 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.25 盒中世界入口、真实投递与跟手拖动更新

- “盒中世界”继续作为工作面板、径向菜单与组件库中的可见入口；进入时会真实呈现独立场景窗口，关闭或最小化后再次进入也会恢复可见界面，而不是只改变后台状态。
- 主动陪伴气泡只有在原生窗口真正可见且已暴露后才确认投递 ACK；呈现超时、安静抑制或进程重启不会误报已读，内容无关的投递记录会恢复为未读并在安全前台重新呈现。
- 人物拖动默认采用 direct 路径：4px 以内仍是点击，越过阈值后按全局逻辑坐标直接跟手；原生 system-move 仅作为明确协商的可选路径，不会造成重复移动或错误完成手势。
- 只读运行快照会严格投影场景、气泡投递与窗口状态，不包含气泡正文；盒中世界呈现、气泡原生 ACK、运行快照与拖动运行级回归均纳入 v0.3.25 发布门禁。
- Windows 版本资源、安装元数据、主题清单与三份发布报告统一升至 v0.3.25；v0.3.24 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.24 静默状态、盒中世界与窗口恢复更新

- 全屏、敏感窗口和受保护界面继续采用 fail-closed 静默：莉莉丝会暂时隐藏，离开抑制状态后自动回来；运行中的专注仍在后台计时，暂停中的专注不会偷偷前进。
- 设置页与托盘现在会说明莉莉丝当前为何隐藏、依附或避让；托盘与专注计时环同时显示专注剩余时间。状态文本只使用固定类别，不读取或泄露窗口标题。
- “盒中世界”在 UI 主线程入口中同步呈现，后台组件调用仍安全排队；关闭后再次进入不会只改变状态而没有窗口。
- 对话信笺从最小化状态恢复时会进行有界稳定检查；窗口已经恢复后不再反复置前、抢回 WPS 等应用的焦点，用户后续主动最小化会被尊重。
- 专注计时环在静默期间隐藏但保持真实状态，重新出现后继续显示准确的剩余时间、已用时间和进度；对应的运行、暂停、恢复与受限场景均有离屏回归门禁。
- Windows 版本资源、安装元数据、主题清单与三份发布报告统一升至 v0.3.24；v0.3.23 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.23 桌面发现、专注反馈与姿态覆盖更新

- 新增只读 Windows 桌面表面诊断，可核对 Lilies 窗口的可见性、层级、所有者与 DWM 隐藏状态；诊断不截图，也不改变任何窗口状态。
- 专注计时环现在同时呈现剩余时间、已用时间与真实进度端点，并明确区分开始、暂停、恢复、自然完成、手动结束与取消；隐藏、暂停和受限状态会停下动画，低功耗状态按秒直接同步。
- 桌宠栖息按极小、小型、中型、大型、最大化与窄标题栏六类策略选择分层姿态，尺寸阈值带滞回，人物接触点、点击范围与红绳端点继续保持可验证对齐；可选新图资产未启用时安全回退到现有分层素材。
- Windows 版本资源、安装元数据、主题清单与发布报告统一升至 v0.3.23；v0.3.22 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.22 主动陪伴可见性与订阅协议兼容更新

- 主动陪伴设置会直接展示运行模式、智能屏幕理解授权状态与下一次最早可能出现的时间，并提供清晰的“试一下：立即说一句”入口；对话、盒中世界与设置仍是径向菜单的三个常驻入口。
- 手动试说仍可绕过自动气泡等待，但不会绕过智能截图的稳定窗口、自然停顿与冷却条件；未授权、敏感窗口或条件不满足时只使用应用类别信号。
- 订阅桥接兼容当前 Codex App Server 的已知状态通知，并在严格校验通知结构后忽略与当前回复无关的全局状态；未知协议方法与未授权工具继续失败关闭。
- Windows 版本资源、安装元数据、主题清单与发布报告统一升至 v0.3.22；v0.3.21 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.21 Windows 启动恢复、桌面切换 ACK、响应式专注与新姿态

- 修复真实 Windows 图形后端启动时的原生命中测试重入；新增无可见窗口、无托盘副作用的打包启动探针，只有原生窗口创建、命中测试分发和 Qt 事件循环均可响应时才允许发布。
- “莉莉丝桌面／仅桌宠”单实例切换改用应用完成 ACK：调用方只有在目标界面真正应用后才报告成功，完整桌面入口不再因仅收到请求便误判完成。
- 专注计时环会随人物与可用工作区响应式布局，专注期间切换到独立的 `focus-watch` 姿态；新增高分辨率跪坐专注姿态，并继续保留各服装的分层兼容回退。
- Windows 版本资源、安装元数据、订阅桥接与发布报告统一升至 v0.3.21；v0.3.20 及更早发布脚本和记录继续作为不可变历史保留。

### v0.3.20 轻量盒核与发布边界更新

- 完整桌面收回盒子后会释放不再可见的 Qt Quick 场景图与图形资源，保留桌宠呼吸、快捷入口和必要后台服务，降低紧凑模式的常驻开销。
- Windows 版本资源、安装元数据、主题清单与发布报告统一升至 v0.3.20；v0.3.19 发布脚本和记录继续作为不可变历史保留。

### v0.3.19 独立呈现与桌面往返更新

- 盒中世界现在由独立场景窗口真实呈现，并补齐小窗、高 DPI 与窄工作区布局验证；入口、场景层和返回路径不再只依赖后端状态。
- 主动陪伴按应用级上下文自动触发，气泡在紧凑窗口中保持可读排版；对话窗口最小化后再次进入会恢复并置前。
- 桌宠拖动继续使用事件坐标与缺帧回退保持跟手，完整桌面支持可靠往返；离屏自测只验证回退与原生路径存在，不冒充 Windows `startSystemMove()` 已在真实指针会话中成功。

### v0.3.18 桌面在场与发布可靠性更新

- 盒中世界升级为可直接进入的独立纸箱场景；主动陪伴、专注计时、人物栖息姿态与桌宠拖动继续收紧交互反馈和边界行为。
- 姿态与服装兼容关系、窗口避让和高 DPI 几何新增离屏验证，避免不可见换装、整图呼吸缩放和贴靠越界。
- Windows 构建新增正式 VERSIONINFO，并强化候选资源、临时目录清理和发布文件完整性门禁。

`Lilies in the box` 是一个可恢复的 Windows 11 桌面外壳、动态主题与莉莉丝 Agent。v0.3「共鸣」把现实事项、莉莉丝陪伴、完成反馈与确定性成长连接成一个本地闭环。

### v0.3.17 主动陪伴订阅桥接更新

- Codex App Server 初始化现在显式开启动态记忆工具所需的实验能力，修复主动气泡请求在创建临时会话时被拒绝、只能退回内置文案的问题。
- 若旧版或受策略限制的 App Server 仍拒绝动态工具，本次气泡会自动以无记忆工具的临时会话重试一次；不会因为可选记忆召回失效而整条气泡消失，也不会执行未声明工具。
- 订阅临时会话显式关闭 Shell、浏览器、Computer Use、图像生成、Apps、Plugins、MCP、Skill 搜索与安装等内建能力；只接收当前 thread/turn 中的回答事件和本次声明的动态记忆工具，未知协议消息立即中止连接。
- 新增无屏幕内容、无用户资料的订阅桥接烟测脚本；它使用生产 `memory.recall` 命名空间和随机 nonce 验证一次真实工具往返，登录失效只报告非敏感账户状态，不输出令牌。
- 工作面板直接点击“盒中世界”页签会同步完整进入状态、标题与页头；“现在说一句”真实按钮和可选径向“陪伴”入口都有可见气泡结果。
- 桌宠拖动帧会优先消费已经到达 QWindow 的原生全局指针事件，再使用 8ms 光标采样补缺帧，减少移动窗口时短暂停顿后追赶的手感。

### v0.3.16 交互可靠性更新

- 桌宠拖动改用每个 Qt 鼠标事件携带的全局逻辑坐标；全局光标兜底只在事件缺帧时介入，旧窗口原点不会再与新位置叠加造成过冲。1×、1.5×、2×及跨屏压力拖动的持点误差保持在约 1 DIP。
- 主动陪伴心跳会在首次前台探测之前启动；Explorer、登录外壳或 User32 的瞬态读取失败不再形成“界面显示开启、实际永不触发”的坏状态。
- 主动陪伴页和功能库使用真实可用宽度布局，窄聊天窗口中的状态、入口和按钮不会落到视口之外；同一气泡窗口换内容时会再次置前，但仍不抢键盘焦点。
- “盒中世界”按钮、原生窗口标题与页内顶栏使用统一名称。面板最小化、停留在旧显示器或落到屏幕外时，再次进入会恢复并带回莉莉丝所在工作区。
- 工作面板不再强制使用超出高 DPI 小工作区的 `720×540` 窗口；窄屏页签缩写、纸卡正文可滚动，200% 缩放下仍能完整到达盒中世界。
- 功能库按稳定 action 标识管理三个可选槽位；对话、盒中世界、设置始终常驻，第四个可选功能在上限处会立即禁用且不会留下假勾选。
- 主动陪伴和模型桥接新增关闭栅栏与有限线程收敛：退出后迟到的生成、内容刷新和能力探测不能再复活气泡或写入会话；旧 Codex reader 不会污染新连接，模型任务诊断历史固定为最多 256 条。
- 默认主动陪伴只把粗粒度应用类别交给已登录的订阅模型，不发送窗口标题、文档名、窗口类或进程名；经明确授权的智能截图会在截图前后分别复核同一 HWND 的密码、支付、隐私和保护状态，状态变化即删除暂存并中止模型调用。
- 对话、设置、陪伴和记忆入口统一恢复已最小化的信笺窗口并置前；Windows+D 后再次点击不再只改变后端状态而看不到界面。
- v0.3.16 发布脚本会校验打包自测版本、紧凑模式资源报告、关键 QML/主题哈希与报告时间；旧 dist 或伪造文件名无法再被提升成新版本。

### v0.3.15 触感与计时更新

- 人物拖动的单击/拖动判定统一使用真实欧氏位移，斜向轻微手抖不会误触发拖动，松手与取消路径也使用同一阈值。
- 拖动时以实际抓取点持续跟随全局逻辑光标；本地事件与 8ms 安全采样交替、200% DPI 跨屏缩放时也不会累积偏移或振荡。
- 仅桌宠模式打开“盒中世界”时，工作面板会可靠出现在当前应用前方；若面板曾被最小化，再次进入会先恢复并直达盒中世界页。
- 主动陪伴的常驻入口会显示当前感知、冷却与暂停状态，新气泡出现时无抢焦点置前；模型不可用时仍可用莉莉丝的本地安全文案回应。
- 专注计时环的圆环、内缩、字号和间距会随可用逻辑尺寸共同缩放；混合 DPI 下的极小工作区不再塌成实心圆。
- 九类窗口姿态的几何过渡统一纳入切换状态；角色缩放、接触点和画布高度动画完成前不会过早报告稳定，最终人物锚点与红绳端点保持一致。
- 新增阅读前倾、展示说明、红绳托盒和蜷坐休息四张真透明姿态；运行时使用审核后的独立裁切、点击框与红绳锚点，不会把相邻姿态切进来。
- 主动陪伴会明确显示频率关闭、冷却、暂停、日上限和本地安全文案退化，不再把实际被拦截的状态写成“允许”；新闻与科研按目标类别选择来源，无来源时给出可见原因。

### v0.3.14 轻量栖息更新

- 仅桌宠冷启动不再构造完整 2D 桌面、4K 图层或视频播放器；视频主题只在真正展开 Lilies 桌面时加载，收回盒子后销毁场景对象。
- `QtMultimedia` 隔离到按需 QML 组件，紧凑冷启动不载入 FFmpeg/Media Foundation；离屏同运行时工作集由约 506MiB 降到约 138MiB。
- 新增面向中型竖屏与近方形窗口的 `portrait-title` 坐姿，并对九类窗口姿态提供可验证的不同倾角、非等比呼吸和露出节奏。
- 竖屏姿态使用纵横比滞回，所有姿态动画继续围绕接触点变换；接触点、红绳端点和公共命中边界不会随呼吸漂移。

### v0.3.13 打磨更新

- 径向功能在出现的第一帧即可点击，人物 3px 手抖仍视为单击，超过 4px 才进入拖动；窗口栖息状态不会在松手后错误抢回位置。
- 菜单与专注计时环会依据当前显示器工作区、负坐标、混合 DPI 和极小窗口重新夹取，不再在屏幕边缘被裁掉。
- 专注计时以计划时长为上限，休眠/卡顿后的自然完成、手动结束和重复结束不会重放动画或重复发放共鸣值。
- 六套服装逐张使用真实宽高比和脚线锚点，换装不再横向拉伸或上下跳动；夏季裙在独立透明成品完成前明确作为初遇裙的视觉别名。
- 固定端口在数据库和桌面创建前完成单实例占用；开始菜单和托盘只唤回当前模式，不再生成第二套莉莉丝或暗中切换桌面状态。
- 新增姿态与服装素材门槛，RGB 棋盘格、假透明、绿边、白边、低分辨率及未审阅重复资源不会接入运行时。

## v0.3「共鸣」

- 分层二头身桌宠会呼吸、栖息在稳定窗口边缘，并在敏感窗口、会议、远程桌面和全屏游戏中自动静默或隐藏。
- 极简白纸 Dock 按应用身份分组真实窗口，显示原生应用图标；多窗口使用 MRU 缩略卡片与独立“全部窗口”抽屉。
- 本地任务、专注、论文阅读和提醒即使模型离线也能运行；专注切到娱乐应用 45 秒后只提醒一次。
- 共鸣值由不可变事件账本计算，任务、专注、阅读、Slack 整理与主动休息会确定性解锁服装、姿态和盒中陈设，失败与延期不扣分。
- Google Calendar 与 Slack 默认关闭。连接后默认采用“必要范围 + 安静 + 元数据 + 协助”；OAuth 使用系统浏览器，令牌只进入 Windows Credential Manager。
- Calendar 修改和 Slack 回复只生成不可变预览，必须在连接器窗口逐次确认后才提交；模型没有直接提交、授权、Token、任意 Shell 或成长授予接口。
- 专注开始后，莉莉丝旁会出现不抢焦点、可穿透点击的呼吸计时环；自然到时、手动结束、取消、暂停和全屏静默使用不同状态，顶部空间不足时计时环自动移到人物侧边。
- 桌宠会按小窗、中窗、大窗、窄标题栏、顶部空间不足和最大化窗口选择坐姿、趴伏、倾听或侧边探身；按 DPI 与真实标题栏高度贴合，并用滞回与锚点插值避免窗口临界尺寸反复跳姿态。
- “莉莉丝桌面／仅桌宠”与“看桌面／返回工作”已明确分开；前者切换外壳，后者只临时收起并恢复本轮窗口。

详细边界与实现说明见 [docs/v0.3-resonance.md](docs/v0.3-resonance.md)。

论文划词继续通过本机 Codex App Server 和当前 ChatGPT 订阅调用 Luna，无需 API Key；主动陪伴优先使用 Luna，图像能力不足时才尝试 Terra。本机已有的 0.5B 仅作离线备用，不会下载新模型。

## 直接运行

已经生成的个人版程序位于：

```text
F:\code\Lilies in the box\dist\LiliesInTheBox\LiliesInTheBox.exe
```

普通入口会恢复上次选择的完整桌面或透明桌宠模式；明确需要完整桌面时使用开始菜单的 `Lilies in the box - Desktop`，需要只保留桌宠时使用 `Pet only`。重复点击这些入口只会通知现有实例切换，不会启动第二个莉莉丝、托盘或数据库连接。关闭界面只会收起，彻底退出请使用托盘中的“退出并恢复 Windows”。

## v0.2 延续能力（v0.3 继续保留）

- 看桌面：按 `Ctrl+Alt+D` 或点击桌宠旋出菜单中的“看桌面”，第一次只临时最小化当前虚拟桌面上原本未最小化的应用，第二次恢复本次由 Lilies 收起且身份仍匹配的窗口和前台焦点。当前原生/Lilies 桌面模式不会切换，快捷键可在设置中修改；崩溃恢复日志位于 F 盘。
- 主动陪伴：内容可选科普、吐槽、笑话、新闻、科研进展与盒中世界；支持关闭、安静、平衡、活泼和自定义频率，兴趣/场景比例、场景半衰期和各类别权重均可调。气泡不抢焦点，可展开、回复、换一个、暂停、对当前应用静默或转入盒子。
- 智能观察：默认关闭。明确开启后，仅在窗口稳定、用户自然停顿且冷却满足时截取一次当前活动窗口；密码、支付、会议、远程桌面、隐私浏览、UAC、受保护内容与全屏游戏默认拒绝。暂存图片在请求完成、失败或取消后删除，只有“保存此刻”才写入图库。
- 实时内容：默认关闭。明确开启后可更新 arXiv、Crossref、PubMed、RSS、NASA/JPL 与可选 GDELT 元数据，只缓存标题、来源、日期、链接和短摘要。新闻与科研气泡必须显示来源和日期，离线或过旧内容不会宣称“最新”。
- 分区记忆：原始对话立即进入 F 盘“待归档”，后台空闲时由 Luna 整理进身份、关系、偏好、项目、研究、日常、世界观等固定分区。模型只能通过只读 `memory.recall` 按需取回最多 6 段；身份与称呼核心事实固定注入。记忆地图可以查看出处和查阅原因、改分区、忘记或经二次确认删除原对话。
- 私有数据：Lilies 创建的数据库、聊天、缓存、截图暂存和运行状态固定在 `F:\code\Lilies in the box\private-data`。F 盘不可用时只执行受限恢复，绝不静默回退写入 C 盘。旧数据经 SQLite Backup API、完整性校验、F 盘备份和两次启动验证后才清理。
- 输入脉冲：只在内存中保留最近 3–10 秒的活跃率、停顿、节奏和量化鼠标方向，不保存字符、按键或窗口标题；允许状态下的光标位置读取最高 30Hz，也不进入数据库、日志或模型上下文。

- 桌面图标：拖动可改位置；右键可以固定、隐藏、分组或打开所在位置。设置页可以复制、切换和删除多套虚拟布局，真实文件不会被移动。
- 桌宠：莉莉丝正面、二头身、抱拳并微微颔首。她使用独立窗口，切换完整桌面时不会被替换或隐藏。默认“始终置顶”，人物、盒子和功能环都可以点击，周围透明区域则穿透到 WPS 或游戏；设置中可切换为“普通窗口”，保留交互但允许其他应用遮挡她。单击人物后，对话、盒中世界、设置 3 个常驻入口与最多 3 个自选入口围绕她旋转展开；选择功能后自动收环。拖动超过 4px 时立即进入精确跟随并收环，不再二次采样光标造成首帧跳动；在人物上滚轮或脚边 `↘` 手柄可调整整体大小。圆形“盒子”可独立拖动、滚轮缩放，红绳端点实时绑定盒缘。
- 功能按钮：设置页的功能库决定 3 个自选槽位；展开后可用右键拖动单个按钮，用滚轮缩放。人物、盒子和按钮的位置会跨重启保存。
- Dock：完整桌面底部默认只保留一枚居中的 64×6 暖白纸边与暗红绳结；悬停后展开为 52px 高的浅纸折边。运行窗口按应用身份分组，最多显示 7 个应用组，同组窗口以数量角标和最多 6 个 MRU 预览呈现；更多窗口进入单层、可搜索、虚拟化滚动的“全部窗口”抽屉。50 窗口场景不会一次创建全部窗口卡片，少量窗口时抽屉会按内容收高，移开后自动折回纸边。
- 对话：GPT Terra 通过当前 ChatGPT/Codex 订阅模拟莉莉丝，回答流式出现；Enter 直接发送，Shift+Enter 换行。支持本地历史搜索、新对话、重试和停止生成。模型先看到分区目录，仅在需要时调用只读记忆检索；明确问“你还记得……”会强制检索一次。
- 应用启动：可以直接对莉莉丝说“打开 WPS”“我要打开 WPS”或“帮我启动一下 WPS”。应用只从桌面和开始菜单的受控索引中选择；索引过期时会自动刷新，遇到多个不同结果会先要求完整名称，不会把对话内容当作任意系统命令执行。
- 文件与网页：可以说“打开下载文件夹”“打开论文.pdf”“打开 F:\\资料\\报告.docx”“打开 GitHub”“访问 example.com”或“上网搜索 莉莉丝”。按名称查找只覆盖桌面、文档、下载和手动添加目录，完整路径可以指向其他现有位置；网页只允许 HTTP/HTTPS。
- 论文卡片：术语卡生成成功后自动保存；简释、翻译和追问只有点“收进盒”才会落入本地 SQLite。盒子的“阅读”页可以搜索原文、回答、标题和问题并按类型筛选。阅读卡不会自动进入主对话或长期记忆。
- 命令边界：v0.3 不注册终端组件，也不接受自然语言或固定前缀执行 Shell；应用、文档、文件夹与网页仅通过各自受限组件打开。
- 身份保护：简单问候和“你是谁”由程序层固定校准；历史中的旧身份错误不会继续注入模型，模型输出中的称呼串位也会被清洗。
- 权限：标准模式允许读取与启动；修改操作需要确认。信任模式可逐项设置写操作白名单，破坏性动作仍需确认。
- 登录外壳：只有视觉模式健康检查通过后才允许手动启用，并在下一次登录生效；Explorer 同时保留为启动失败回退。
- 性能：实时场景和电影循环都会报告真实 FPS；完整桌面被最大化窗口完全遮挡时暂停动画/视频。0.5B 工作进程使用后台优先级，避免抢占桌面渲染。

姿态、服装、UI 方向稿与后续高价值功能顺序记录在 [docs/v0.2-evolution.md](docs/v0.2-evolution.md)。

## 安全说明

- 默认视觉外壳只隐藏 Explorer 桌面图标和任务栏，不结束 Explorer。
- 独立 watchdog 会在主程序异常退出后恢复 Windows 桌面。
- 实验性登录外壳不会自动启用，必须在设置中明确确认。
- “退出并恢复 Windows”会先恢复任务栏和桌面，再结束程序。
- 智能截图和联网内容源均默认关闭，必须分别在“陪伴”页明确开启。
- 所有屏幕文本、历史对话与网页摘要都按不可信数据处理，不能扩大组件权限或触发终端。

## 开发运行

```powershell
cd "F:\code\Lilies in the box"
.\scripts\setup_windows.ps1
.\scripts\run_windows.ps1
```

需要重新生成个人版程序时：

```powershell
.\scripts\build_windows.ps1
```

这是一键发布证据构建：完成 PyInstaller 后会按依赖顺序重新生成并检查以下四份报告，任一步失败都会停止，旧报告会先被安全移除：

1. `artifacts\packaged-self-test-v0350.json`
2. `artifacts\packaged-compact-resource-v0350.json`
3. `artifacts\packaged-windows-startup-v0350.json`
4. `artifacts\pose-click-mask-v0350.json`

需要逐步排查时，可用与一键脚本相同的顺序手动执行：

```powershell
& .\dist\LiliesInTheBox\LiliesInTheBox.exe --self-test .\artifacts\packaged-self-test-v0350.json
.\scripts\verify_packaged_compact_resources.ps1
.\scripts\verify_packaged_windows_startup.ps1
& .\.venv\Scripts\python.exe .\scripts\verify_pose_click_masks.py
.\scripts\promote_v0350.ps1 -ValidateOnly
```

最后一条只验证、不复制：它与正式 promotion 使用同一套 EXE 版本、AdditionalFiles、三份打包报告、姿态点击报告与内容无关原生捕获探针门禁。只有全部通过后才运行不带 `-ValidateOnly` 的正式提升。

## Calendar / Slack 初次连接

- Calendar：在 Google Cloud 创建个人 Desktop OAuth Client，只把 Client ID 填入连接向导，再由系统浏览器授权。默认只读；将协助档位手动升级为“确认执行”后，才会重新申请 owned-events 写权限。
- Slack：先由设置页生成 manifest，在 Slack 创建个人 custom app，启用 Socket Mode，并创建只含 `connections:write` 的 xapp token；再填写 Client ID、`127.0.0.1` 回调地址、本人 Slack User ID、xapp token 与可选频道 ID 后授权。
- 两个连接器都不使用 OpenAI API Key，但需要你自己的 Google/Slack OAuth 应用；组织管理员可能拒绝 Slack 权限。
- 默认“元数据 + 协助”只表示协助能力可用，元数据档并不保留正文，所以无法总结或拟稿。先把留存升为“可搜索摘要”或“扩展缓存”，再明确点选一项；每次只把该项与本次指令交给 Luna，不附带相邻项目、长期记忆或组件权限。

## 安装与卸载

安装到当前用户的 `%LOCALAPPDATA%\Programs\Lilies in the box`：

```powershell
.\scripts\install_windows.ps1 -StartAtLogin
```

安装脚本默认启动上次由你选择的桌面/桌宠模式，不再强制紧凑模式。开始菜单同时提供普通入口、明确展开完整桌面的 `Desktop` 入口和 `Pet only` 入口；三者都先激活正在运行的单实例。若不希望安装后立即启动，使用：

```powershell
.\scripts\install_windows.ps1 -StartAtLogin -NoLaunch
```

卸载程序并恢复 Windows；默认保留本地聊天与记忆：

```powershell
.\scripts\uninstall_windows.ps1
```

只有明确需要清除 F 盘私有数据和仍残留的 v0.1 迁移副本时才使用：

```powershell
.\scripts\uninstall_windows.ps1 -RemoveData
```

安全冒烟（不隐藏 Windows 桌面）：

```powershell
.\scripts\run_windows.ps1 --smoke
```

紧急恢复：

```powershell
.\scripts\restore_windows.ps1
```

## 莉莉丝对话模型

主对话使用 `gpt-5.6-terra`（medium），复用当前电脑上 Codex 的 ChatGPT 订阅登录，不需要 OpenAI API Key。每次生成都使用临时会话；固定身份、分区目录和按需召回片段从本地 SQLite 注入，对话数据不写入 Codex 历史。空闲五分钟后关闭连接。

电脑中已有的 `Qwen2.5-0.5B-Instruct` safetensors 检查点继续通过独立 CPU 进程保留；在提交一轮对话前检测到断网、订阅退出或 GPT 不可用时由它离线接替，不下载或扫描其他模型。已经提交给 GPT 的一轮若在生成中失败，会明确提示重试，不会在同一任务锁内悄悄跨模型续写。主动陪伴不会持续观察桌面；只有用户明确开启智能观察、隐私策略允许且窗口满足稳定/停顿条件时，才向支持图像的 Luna 或 Terra 发送一次当前活动窗口。模型动作只能访问注册过的组件；v0.3 不向模型或对话输入公开任意 Shell，组件结果由程序直接呈现，不交给模型改写。

检查现有离线备用 0.5B 是否可用：

```powershell
.\scripts\install_model.ps1
```

这个脚本只做本机检查，不安装也不下载模型。
